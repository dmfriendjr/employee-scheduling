{{!-- 
////////////////////
//////Nav Bar///////
//////////////////// 
--}}

<div id="account-background"></div>

{{!-- <nav id="navigation" class="navbar navbar-toggleable-md navbar-light">

  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon">&nbsp;</span>
  </button>

  <h3 class="navbar-header mr-auto account-page-font">EmployEase</h3>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav d-flex justify-content-end">

      <li class="nav-item">
        <a class="nav-link" href="/manageemployees">
          <i class="fas fa-users"></i> Company Overview
        </a>
      </li>

      <li class="nav-item">
        <a class="nav-link" href="/scheduling">
          <i class="fas fa-users"></i> Scheduling
        </a>
      </li>

    </ul>

    <div class="navbar-right disabled">
      Signed in as {{user.username}}
    </div>

  </div>
</nav> --}}

<nav id="navigation" class="navbar navbar-default">
  <div class="container">
    <h3 class="navbar-header account-page-font">EmployEase</h3>
    <div class="navbar-right dropdown account-page-font">
      Signed in as
      <span class="dropdown-toggle nav-name-dropdown" data-toggle="dropdown">{{user.username}}</span>
      <span class="caret"></span>
      </a>
      <ul class="dropdown-menu">

        <li>
          <a href="/ManageEmployees" class="dropdown-link">
            <i class="fas fa-users"></i> Employees
          </a>
        </li>

        <li>
          <a href="/scheduling" class="dropdown-link">
            <i class="far fa-address-card"></i> Schedule
          </a>
        </li>

        <li>
          <a href="/logout" class="dropdown-link">
            <i class="fas fa-sign-out-alt"></i> Logout
          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>


<div class="container-fluid">

  {{{message}}}

  <div class="row">
    <div id="new-employee" class="col-md-3 pt-2">
      <h4 class="text-right">New Employee</h4>
      
      <form id="new-employee-form" method="post" class="form-group" action="/employees">
        <div class="form-group">
          <input class="form-control employee-form-control text-right border-bottom col-8 offset-4" 
            type="text"
            placeholder="John Doe"
            name="name" 
            value={{employeeName}}>
        </div>

        <div class="form-group">
          <input class="form-control employee-form-control text-right border-bottom col-8 offset-4" type="text" placeholder="555-555-5555" name="phone_number" value={{employeePhone}}>
        </div>

        <div class="form-group">
          <input class="form-control employee-form-control text-right border-bottom col-8 offset-4" type="email" placeholder="john@doe.com" name="email" value={{employeeEmail}}>
        </div>

        <button class="invisible-button float-right" type="submit"><i class="fas fa-plus"></i> Add Employee</button>
      </form>



    </div>
    <div class="col-md-7 offset-1 col-12">
      <h1 class="text-center pt-2 mb-4 text-capitalize">Howdy, {{user.username}}</h1>
      <h4 class="text-center text-capitalize">{{user.company}} Employee Overview</h3>
          <table id="employeeTable" class="table">
            <thead>
              <tr >
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Phone</th>
                <th scope="col">Email</th>
                <th scope="col">Remove</th>
              </tr>
            </thead>
            <tbody>
              {{#each employees}}
              <tr>
                <th scope="row">{{id}}</th>
                <td>{{name}}</td>
                <td>{{phone_number}}</td>
                <td>{{email}}</td>
                <td id={{id}}><button class="btn btn-danger deleteEmployee">Delete</button></td>
              </tr>
              {{/each}}
            </tbody>
          </table>
    {{!-- </div> --}}
  </div>
</div>

<script>
  $('.deleteEmployee').on('click', (event) => {
    console.log(event.target.parentNode.id);
    $.ajax({
      method: 'DELETE',
      url: '/employees',
      contentType: 'application/json',
      data: JSON.stringify({
        id: event.target.parentNode.id
      }),
    }).then(() => {
      location.reload();
    });
  });
</script>